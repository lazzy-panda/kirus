const fetch = require('./lib').default;

console.log(fetch);

const links = [
      'http://localhost:3000',
      // 'http://data.bnf.fr',
      // 'http://www.cnrs.fr/fr/recherche/index.htm',
      // 'https://www.google.fr/',
      // 'https://nodejs.org/api/http.html',
];


links.forEach(link => fetch(link)
        .then(function (response) {
                    return response.text();
                })
        .then(console.log)
      .catch(console.error));
